/// Durações de animação padrão
/// Define os tempos de animação para consistência no app
library;

// ==================== DURAÇÕES ====================

/// 75ms - Animação muito rápida
const Duration durationFast = Duration(milliseconds: 75);

/// 150ms - Animação rápida (padrão)
const Duration durationNormal = Duration(milliseconds: 150);

/// 300ms - Animação média
const Duration durationMedium = Duration(milliseconds: 300);

/// 500ms - Animação lenta
const Duration durationSlow = Duration(milliseconds: 500);

/// 1000ms - Animação muito lenta
const Duration durationVerySlow = Duration(milliseconds: 1000);

// ==================== DURAÇÕES PARA TOASTS/SNACKBARS ====================

/// 2 segundos - Toast curto
const Duration toastShort = Duration(seconds: 2);

/// 4 segundos - Toast médio (padrão)
const Duration toastMedium = Duration(seconds: 4);

/// 6 segundos - Toast longo
const Duration toastLong = Duration(seconds: 6);
