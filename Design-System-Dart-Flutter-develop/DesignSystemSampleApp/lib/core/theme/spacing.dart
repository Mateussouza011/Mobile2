/// Sistema de espaçamento shadcn/ui
/// Base de 4px (0.25rem) - múltiplos de 4 para consistência
library;

// ==================== ESPAÇAMENTOS ====================

/// 0px - Sem espaçamento
const double spacing0 = 0.0;

/// 4px (0.25rem) - Espaçamento mínimo
const double spacing1 = 4.0;

/// 8px (0.5rem) - Espaçamento extra pequeno
const double spacing2 = 8.0;

/// 12px (0.75rem) - Espaçamento pequeno
const double spacing3 = 12.0;

/// 16px (1rem) - Espaçamento base/padrão
const double spacing4 = 16.0;

/// 20px (1.25rem) - Espaçamento médio-pequeno
const double spacing5 = 20.0;

/// 24px (1.5rem) - Espaçamento médio
const double spacing6 = 24.0;

/// 32px (2rem) - Espaçamento grande
const double spacing8 = 32.0;

/// 40px (2.5rem) - Espaçamento extra grande
const double spacing10 = 40.0;

/// 48px (3rem) - Espaçamento 2x grande
const double spacing12 = 48.0;

/// 64px (4rem) - Espaçamento 3x grande
const double spacing16 = 64.0;

/// 80px (5rem) - Espaçamento 4x grande
const double spacing20 = 80.0;

/// 96px (6rem) - Espaçamento 5x grande
const double spacing24 = 96.0;

// ==================== PADDINGS COMUNS ====================

/// Padding horizontal padrão para containers
const double paddingHorizontal = spacing4;

/// Padding vertical padrão para containers
const double paddingVertical = spacing4;

/// Padding para seções grandes
const double paddingSection = spacing6;

/// Padding para páginas
const double paddingPage = spacing6;

// ==================== GAPS COMUNS ====================

/// Gap pequeno entre elementos relacionados
const double gapSmall = spacing2;

/// Gap médio entre elementos
const double gapMedium = spacing4;

/// Gap grande entre seções
const double gapLarge = spacing8;
